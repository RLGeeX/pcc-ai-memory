# Local .gitignore for pcc-ai-memory repository
# This file overrides global gitignore patterns to allow AI tool files to be tracked
# in this AI memory repository since these files are the content we want to preserve

# Override global gitignore to include Claude Code files
!.claude/
!.claude/**
!CLAUDE.md

# Override global gitignore to include Warp files  
!.warp/
!.warp/**
!WARP.md

# Override global gitignore to include other AI agent files
!.agents
!AGENTS.md
!AGENT.md

# Override global gitignore to include Cursor files
!.cursor/
!.cursor/**
!.cursorignore
!.cursorban
!.cursorrules.md

# Override global gitignore to include Continue.dev files
!.continue/
!.continue/**

# Override global gitignore to include Aider files
!.aider*
!.aider.cache/
!.aider.history/
!.aider.tags.cache/